# AI Task Automation API

A simple Python FastAPI project for task automation.

## Features
- REST API
- Background task processing
- Automation-ready structure

## How to Run
1. Install dependencies: pip install -r requirements.txt
2. Run server: uvicorn app.main:app --reload
